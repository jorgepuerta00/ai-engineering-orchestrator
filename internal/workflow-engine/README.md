# Workflow Engine (Go, deterministic)

- `engine/`: orchestration runtime loop
- `definitions/`: workflow/task definitions
- `handlers/`: executable action handlers
- `state/`: persisted run state transitions
- `transitions/`: transition guards and policies
